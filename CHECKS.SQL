SELECT * FROM EST_PRODUCTO; -- OK
SELECT * FROM EST_ESTADO; -- OK
SELECT * FROM EST_TIEMPO; -- OK
SELECT * FROM EST_CLIENTE; -- OK

--CLIENTES CON ERROR
SELECT DISTINCT ID_CLIENTE FROM ORDENES WHERE ID_CLIENTE NOT IN (SELECT ID_CLIENTE FROM CLIENTE);
SELECT DISTINCT ID_CLIENTE FROM CLIENTE WHERE ID_CLIENTE NOT IN (SELECT ID_CLIENTE FROM ORDENES);

SELECT * FROM EST_ORDENES; -- OK

-- REVISION DE CLIENTES INEXISTENTES OK
SELECT DISTINCT ID_CLIENTE FROM EST_ORDENES WHERE ID_CLIENTE NOT IN (SELECT ID_CLIENTE FROM EST_CLIENTE);
SELECT DISTINCT ID_CLIENTE FROM EST_CLIENTE WHERE ID_CLIENTE NOT IN (SELECT ID_CLIENTE FROM EST_ORDENES);

-- REVISION DE PRODUCTOS INEXISTENTES
SELECT DISTINCT ID_PRODUCTO FROM EST_ORDENES WHERE ID_PRODUCTO NOT IN (SELECT ID_PRODUCTO FROM EST_PRODUCTO);
SELECT DISTINCT ID_PRODUCTO FROM EST_ORDENES WHERE ID_PRODUCTO NOT IN (SELECT ID_PRODUCTO FROM EST_PRODUCTO);